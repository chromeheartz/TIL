## Express.js

### 10. 경로 필터링

---

### 📌 경로 필터링

`Express Router` 에는 주목할 기능이 하나 더 있다.
`app.use(adminRoutes)` 로 `아웃소싱 라우트` 들을 만들었는데, 이는 시작 경로를 공유하는 경우가 있다.

예를 들어 모든 `admin 라우트` 가 `/admin/add-product` 로 트리거될수도있는 것이다.
메소드가 다르기 때문에 경로를 반복해도 서로 다른 두 라우트가 되는 것이다.

> 여기서 메소드가 다르면 같은 경로를 사용할 수 있음을 알 수 있다.

이렇게 라우트 파일의 경로가 `같은 구간` 에서 실행하는 경우에는 `app.use` 부분에서 해당 구간을 필터로 추가할 수 있다.`app.use('/admin', adminRoutes);`

```js
// app.js
const express = require("express");
const adminRoutes = require("./routes/admin.js");
const shopRoutes = require("./routes/shop.js");
const bodyParser = require("body-parser");

const app = express();

app.use(bodyParser.urlencoded({ extended: false }));

app.use("/admin", adminRoutes);
app.use(shopRoutes);

app.use((req, res, next) => {
  res.status(404).send("<h1>page not found</h1>");
});

app.listen(3000);
```
