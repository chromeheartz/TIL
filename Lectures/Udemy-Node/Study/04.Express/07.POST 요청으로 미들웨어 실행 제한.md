## Express.js

### 07. POST 요청으로 미들웨어 실행 제한

---

### 📌 POST 요청으로 미들웨어 실행 제한

현재로서는 미들웨어가 `POST` 요청 뿐만 아니라 `GET` 요청에 대해서도 실행된다.
이를 어떻게 처리하면 될까. `app.use` 대신에 `app.get` 을 사용할 수도 있는데 이는 기본적으로 `app.use` 와 동일하고 같은 구문이다.
경로를 사용하거나 사용하지 않을 수 있고 들어오는 `GET` 요청에만 작동한다.

이는 경로 필터가 아닌 다른 형태의 필더라고 할 수 있다.
`app.get` 은 `GET` 요청을 걸러낼 수 있게 해준다. 같은 페이지에 들어오는 `POST` 요청을 걸러주는 것은 `app.post` 가 있다.
이 단어 하나를 바꿈으로써 이 미들웨어는 이제 이 경로에 해당되는 들어오는 `POST` 요청의 경우만 실행되고 `GET` 요청에는 반응하지 않는다.

```node
app.post("/product", (req, res, next) => {
  console.log(req.body);
  res.redirect("/");
});
```

이제 이렇게 하고 저장해서 확인해보면 `GET` 요청에는 반응하지 않는다.
따라서 `/product` 를 입력해도 GET요청이므로 도달하지 않게 되는것이다.

> 모든 `http` 메소드에 반응하는 `use` 대신에 `get / post` 를 사용해서 해당 요청만 걸러낼 수 있다.
