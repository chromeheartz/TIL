## Express.js

### 04. Express.js 백그라운드 확인

---

### 📌 백그라운드 확인

`Express` 의 내부 원리를 조금 들여다보자.
`github` 의 `express repository` 를 확인해보자.
너무 깊게 분석하지는 않겠지만 일부 기능의 작동원리를 확인해보자. `lib` 폴더 안에 `response.js` 파일이 있는데, `send(` 로 검색해보면 호출했던 함수인 `send` 함수가 내부적으로 어떻게 정의되어있는지 볼 수 있다.

`text html` 로 헤더가 되어있을때 내부에서 어떻게 동작하는지 알아보자.
일단 `switch` 로
![](https://velog.velcdn.com/images/chromeheartz/post/f7e8f9d6-3ff3-42af-aefd-0f959a61a02f/image.png)

타입을 구분하고 있는데 현재 입력한 텍스트와 같은 `문자열 데이터` 의 경우 콘텐츠 유형을 `html` 로 설정하는 것을 볼 수 있다.
콘텐츠 유형 헤더가 존재하는지 확인하고 존재하지 않는다면 자동으로 정해주고 있다면 그대로 둔다.
`number / boolean` 등 다른 값의 경우에는 `이진수 / json data` 로 설정한다.

이렇게 들여다보는 것이 도움이 되기도 한다.

`app` 을 현재 `createServer` 메소드로 보내고 있지만 대신 `app.listen` 으로 호출해도 된다.
이는 공식코드의 `application.js` 파일을 들어가서 `listen` 을 검색해보면 된다.
`listen` 함수는 결국 이전에 했던 두 가지 작업을 해준다.

![](https://velog.velcdn.com/images/chromeheartz/post/a28373b4-068d-4912-ac10-a2714eaa10ef/image.png)

`http.createServer` 를 호출하고 자신을 전달한다. 따라서 이전에 전달했던 앱 객체를 `createServer` 로 전달해주고 최종적으로는 해당 서버 객체에서 listen이 호출되도록 해준다.
따라서 내부적으로 간결하게 코드를 쓸 수 있게 되는것이다.

```js
// app.js
const express = require("express");

const app = express();

app.use((req, res, next) => {
  console.log("middle ware called");
  next();
});
app.use((req, res, next) => {
  console.log("In another middleware!");
  res.send("<h1>Hello from Express!</h1>");
});

app.listen(3000);
```
